import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const HomeProfessor = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
        backgroundColor: "white",
      }}>
      <Text>Home!Professor</Text>
    </View>
  );
};

export default HomeProfessor;