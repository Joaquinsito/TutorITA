import React, { useState } from 'react';
import { Text, View, ScrollView, HStack, Avatar } from 'react-native';

const Asesorias = ({ route }) => {
    return (
        <ScrollView>
            <View w="100%">
                <Text>Asesorias</Text>
            </View>
        </ScrollView>
    );
}

export default Asesorias;